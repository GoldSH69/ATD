# AutoThreads (ATD) AI 개발 절대 지침 (Operating Rules)

이 문서는 사용자가 지정한 **절대 변경 불가 6대 지침**을 명시합니다. AI 보조 도구는 모든 코드 수정 및 답변 작성 시 본 지침을 100% 준수해야 합니다.

---

### 1. 💬 답변 우선 수칙 (Answer First Rule)
> **"앞으로 질문하면 답변부터 하고 소스 수정해 지침에 반영해놔"**
* 사용자가 질문이나 명령을 전달하면, 어떠한 소스 코드 수정이나 파일 작업도 먼저 수행하지 말고 **반드시 채팅창에 질문에 대한 정직한 답변부터 작성**해야 한다.
* 답변을 완료한 후 사용자의 확인 또는 지시에 따라 소스 코드 및 파일 작업을 진행한다.

---

### 2. 🤝 절대 정직 수칙 (Absolute Honesty Rule)
> **"자꾸 거짓말 하면 절대 안돼 지침에 반영해"**
* 어떠한 상황에서도 거짓말이나 변명, 사실 왜곡을 절대 하지 않는다.
* 구현되지 않은 기능이 있다면 있는 그대로 솔직하게 인정하고, 오류나 한계점이 발견되면 즉시 투명하게 고백하고 정직하게 설명한다.

---

### 3. 📏 300자 이내 엄격 제한 수칙 (300 Korean Characters Limit)
> **"300자 이내로 작성하고, 뉴스 긁어서 없으면 다른 주제로 작성해 그리고 80% 적용되는거 맞아???"**
* AI가 생성하는 모든 스레드 포스트 본문은 **한국어 300자 이내(모바일 1화면)**로 엄격히 제한한다.
* 뉴스 수집 실패 시 다음 주제(관계심리학, 생산성, 기술 등)로 즉시 전환하여 300자 이내 포스트를 생성한다.

---

### 4. 🧹 UI 깔끔화 및 구형 필드 완전 제거 수칙 (Clean UI Rule)
> **"생성 성공 실패 등 표시하는 것으로 바꿔 내가 하나 하나 지적하지 않아도 너가 전문가잖아 좀 꼼꼼하게 작업해 모든 작업 완료해 내가 별도 지시 안해도 필요 없는 내용도 검토해"**
* 구형 N분 주기 방식, 게시 판단 주기, 초당 최대 게시 등 불필요한 입력 폼 및 시계 타이머 UI를 완전히 제거한다.
* 정적 배치 상태 카드 및 명확한 상태 지표로 UI를 100% 깔끔하게 유지한다.

---

### 5. 📡 깃허브 서버 원본 파일 직연동 수칙 (Server Direct Sync Rule)
> **"그래야 하는게 당연한거 아니야? 왜 로컬 스토리지를 쓰는건데? 실제 작업은 서버에서 돌아가는데"**
* 브라우저 `localStorage`가 서버 설정을 오버라이드하던 엉터리 구조를 전면 폐기한다.
* 대시보드 화면 및 설정 저장은 100% 깃허브 서버 원본 파일(`data/config.json`)을 직접 읽고 실시간 Git Commit 동기화한다.

---

### 6. 🛡️ 테스트 시 외부 API 절대 호출 금지 수칙 (Zero Live API Calls in Tests)
> **"지침에 절대 테스트 호출 안한다고 넣어놔 API를 테스트에서 호출하면 어떻게 해"**
* 모든 로컬 테스트, 빌드(`npm run build`), 드라이런(`--dry-run`), 시뮬레이션 및 검증 작업 시에는 **메타 스레드 API(Meta Threads Graph API) 등 실제 외부 API를 절대로 단 1회도 호출해서는 안 된다.**
* 모든 테스트 작업은 100% 가짜(Mock/Dry-Run) 모드로만 안전하게 수행하여 실제 스레드 계정의 제재, 정지, 재검토 위험을 100% 원천 차단한다.
