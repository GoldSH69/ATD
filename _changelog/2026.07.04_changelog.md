# Changelog — 2026.07.04

## Release 0.1.7 — README Polish + Image Preview Fix

### Fixed
- Remote image previews now render in the draft image picker by allowing HTTP/HTTPS image sources in the renderer Content Security Policy. (`index.html`)

### Changed
- README now uses the provided `src/assets/banner.png` hero and cleaner sanitized screenshots without distracting grid borders or clipped UI text. (`README.md`, `docs/assets/`)

## Release 0.1.6 — AutoThreads Logo

### Added
- Added the generated AutoThreads logo as the in-app sidebar mark and packaged app icon. (`src/assets/autothreads-logo.png`, `build/icon.icns`, `build/icon.ico`, `electron-builder.yml`)

## Release 0.1.5 — Image Search Robustness Patch

### Fixed
- Image search no longer fails when the AI suggests a long keyword phrase. AutoThreads now trims generated keywords, splits long searches into shorter Commons query variants, merges unique image results, and returns usable options for broad technology/news topics. (`electron/images.ts`)

## Release 0.1.4 — News Table Layout Patch

### Fixed
- Restored the News page to a proper table-like vertical layout with explicit headline, source, age, and action columns. Rows no longer render as malformed flex children inside the no-padding view body. (`src/components/NewsView.tsx`, `src/styles/app.css`)

## Release 0.1.3 — Image Picker, Provider Expansion, Token-First Threads Setup

### Added
- Draft editor optional-image workflow: the AI suggests image-search keywords, AutoThreads searches Wikimedia Commons, and the user can select a public image to include with the Threads post. (`electron/images.ts`, `src/components/DraftsView.tsx`)
- Hacker News aggregation alongside Google News for topic-based scraping. (`electron/news.ts`)
- Gemini provider using Google Gemini's OpenAI-compatible chat completions endpoint. (`electron/llm.ts`)
- More robust custom “Other” provider with base URL, model, optional bearer key, headers JSON, and request JSON overrides for sampling/provider-specific fields. (`electron/llm.ts`, `src/components/SettingsView.tsx`, `src/components/Onboarding.tsx`)
- Language selector in onboarding and settings, with first-party localization for the app shell and main workflows in English, Spanish, Korean, Chinese, Japanese, French, German, and Portuguese. (`src/i18n.ts`)
- Access-token help modal with concise Threads token-generation steps. (`src/components/ThreadsTokenHelp.tsx`)

### Changed
- Threads setup is token-first; App ID/App Secret/redirect URI OAuth fields are now tucked into optional advanced setup. (`src/components/SettingsView.tsx`, `src/components/Onboarding.tsx`)
- Settings navigation now warns with a modal when leaving with unsaved changes, offering Save and leave, Discard, or Stay. (`src/components/SettingsView.tsx`, `src/components/Sidebar.tsx`)

## Release 0.1.2 — Larger Draft Editor Metrics

### Changed
- Draft editor now fills the available detail pane height so generated content is much easier to review and edit. (`src/styles/app.css`)
- Draft footer now shows character count, word count, and estimated Threads post count against the 500-character limit. (`src/components/DraftsView.tsx`)

## Release 0.1.1 — Threads OAuth + Jarvis 12B v2

### Added
- Threads OAuth setup in the Electron main process, including local callback listener, authorization-code exchange, long-lived token exchange, and encrypted storage for app secret/access token metadata. (`electron/threadsOAuth.ts`, `electron/main.ts`, `electron/settings.ts`)
- Settings and onboarding controls for App ID, App Secret, redirect URI, scopes, and **Connect with Threads OAuth**. (`src/components/SettingsView.tsx`, `src/components/Onboarding.tsx`)

### Changed
- Local LLM defaults now target the Jarvis local OpenAI-compatible endpoint at `http://127.0.0.1:8080/v1/chat/completions`.
- Default local model is now Jarvis 12B v2: `gemma4-v2`.
- Local LLM adapter accepts either a full `/v1/chat/completions` URL or a base `/v1` URL.

## Session: Build AutoThreads — Threads post-automation studio with local/cloud LLM (10:32)

Brand-new sibling app to AppTest, same stack (Electron + Vite + React 19 + TypeScript, zero native deps), built in `c:\_sites\autothreads-locallm`.

### Added
- **App foundation** — Electron main/preload, dev/build/start scripts, tsconfig split, monotone light/dark design system mirroring the AppTest scaffold. (`electron/`, `scripts/`, `src/styles/app.css`)
- **LLM provider layer** — adapters + connection tests for Claude API, OpenAI API, and any local OpenAI-compatible server (Ollama/LM Studio). (`electron/llm.ts`)
- **Threads Graph API client** — two-step publish, reply publishing, my-posts fetch, style-sample scrape, and unanswered-reply detection. (`electron/threadsApi.ts`)
- **News scraper** — Google News RSS per topic, regex-parsed, degrades to `[]`. (`electron/news.ts`)
- **Generation pipeline + scheduler** — voice-aware prompt building, draft post/reply generation, background publisher for due scheduled posts, and optional auto-drafting. (`electron/pipeline.ts`, `electron/scheduler.ts`)
- **Five views** — Drafts (master/detail editor), News, Replies, Queue, Settings. (`src/components/`)
- **Onboarding setup wizard** — modal with left step-nav, per-step "Save and continue" / "Previous", live connection tests, and a summary; rerunnable from Settings. Shown on first run via a new `onboarded` setting. (`src/components/Onboarding.tsx`)
- **Secret encryption at rest** — API keys + Threads token encrypted via Electron `safeStorage` (DPAPI/Keychain). (`electron/settings.ts`)
- **Open-source docs** — full GitHub README + MIT license. (`README.md`, `LICENSE`)

### Fixed
- 22 adversarially-confirmed review findings (2 critical, 8 major, 12 minor). Notable:
  - Claude generation sent `temperature: 0.8`, which the default model rejects with HTTP 400 → dropped it, raised `max_tokens`. (`electron/llm.ts`)
  - LLM response body was read after the abort timer cleared → a stalled stream hung generation forever; body now consumed inside the timeout window. (`electron/llm.ts`)
  - Scheduler could publish a draft the user unscheduled mid-tick, and a racing renderer upsert could roll back a `posting` draft → re-check due-status per publish, protect scheduler-owned statuses, sanitize all draft IPC input. (`electron/scheduler.ts`, `electron/drafts.ts`)
  - Empty/over-limit scheduled drafts retried every 30s forever → now fail once and surface. (`electron/scheduler.ts`, `src/components/DraftsView.tsx`)
  - Emptied topics list silently resurrected to defaults on save → persists empty. (`electron/settings.ts`)

### Security
- `will-navigate` allowlist compared string prefixes (`http://localhost:5174@evil.com` bypassed it) → now compares parsed URL origins. (`electron/main.ts`)
- Draft IPC input sanitized/coerced before it can reach the scheduler; local LLM base URL validated as a proper http/https URL. (`electron/drafts.ts`, `electron/llm.ts`)

### Docs
- README documents full functionality, provider setup, Threads token scopes, architecture diagram, and project layout for open-source release.

### Open / next
- Package/sign installers (electron-builder) for Windows + macOS distribution — not yet set up.
- Threads reply pagination beyond 100 items and `/conversation` availability vary by token; verify against a live account before relying on auto-reply at scale.
- Residual: a compromised renderer could still use the main process to reach a user-configured local URL (inherent to the local-LLM feature); mitigated by input validation and context isolation.
